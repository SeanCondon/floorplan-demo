import { SitePlanDirective } from './site-plan.directive';

describe('SitePlanDirective', () => {
  it('should create an instance', () => {
    const directive = new SitePlanDirective();
    expect(directive).toBeTruthy();
  });
});
